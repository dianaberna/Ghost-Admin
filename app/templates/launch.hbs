<div class="fullscreen-wizard-container">
    <button type="button" class="close absolute top-6 right-6 w6" title={{t "common.Close"}} {{on "click" this.close}} data-test-button="close-wizard">
        {{svg-jar "close"}}
        <span class="hidden">{{t "common.Close"}}</span>
    </button>

    {{outlet}}
</div>